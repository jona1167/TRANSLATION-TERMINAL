{
  "name": "translation-terminal",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "start": "bun server.ts",
    "build": "bun build.ts",
    "build:host": "bun build.ts --host-only"
  }
}
