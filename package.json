{
  "name": "translation-terminal",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "start": "bun server.ts",
    "build": "bun build.ts",
    "build:host": "bun build.ts --host-only"
  }
}